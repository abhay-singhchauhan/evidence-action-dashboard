# Water monitoring dashboard

Open `index.html` in a browser. Site data comes from `data/sites.js`. The chart uses local Chart.js in `vendor/`.

I used AI help to build this because I had limited time.
